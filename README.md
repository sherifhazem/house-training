نظام إدارة مربط جادا للأصالة (Jada Stables Management System) 🐎

نظام ويب متكامل واحترافي مصمم لمتابعة وتحليل عمليات تدريب الخيل اليومية، مراقبة الحالة الصحية، وإدارة السجلات العامة والشهادات الرسمية للمربط. يعتمد النظام على تقنيات الويب الحديثة والربط المباشر مع بيانات "جداول بيانات جوجل" (Google Sheets).

📊 التحليل التقني للمشروع

يعتمد المشروع على فلسفة "تطبيقات الصفحة الواحدة" (SPA) حيث يتم معالجة كافة البيانات في المتصفح (Client-side) لضمان السرعة الفائقة واستقرار الأداء دون الحاجة لخادم (Server) معقد.

🛠️ التقنيات المستخدمة

Tailwind CSS: لبناء واجهة مستخدم (UI) عصرية ومتجاوبة (Responsive) تدعم الجوال والكمبيوتر بذكاء.

ApexCharts.js: لتوليد الرسوم البيانية التفاعلية (منحنيات الأداء، مؤشر الصحة، توزيع التمارين).

PapaParse: لمعالجة ملفات CSV المستلمة من Google Sheets بسرعة وكفاءة.

html2pdf.js: لتحويل تقارير الأداء إلى ملفات PDF احترافية جاهزة للطباعة.

Lucide Icons: لتوفير نظام أيقونات بصري واضح وسلس.

🚀 المميزات الرئيسية

1. تبويب تحليل التدريب (Analysis Dashboard)

مؤشرات الأداء (KPIs): عرض فوري لإجمالي الحصص، متوسط النشاط، عدد ساعات التدريب، ونسبة سلامة القطيع.

تقويم النشاط (Activity Calendar): تقويم ذكي يبدأ من يوم السبت (يتوافق مع أسبوع العمل العربي)، يظهر أيام التدريب المظللة وأيام الراحة لتسهيل رصد "الخمول".

منحنيات الأداء: رسم بياني يوضح تطور مستوى نشاط الخيل عبر الزمن لرصد أي هبوط أو تحسن في الأداء.

مؤشر الكفاءة الصحية: عداد (Gauge) تفاعلي يحلل الحالة الصحية العامة بناءً على المدخلات اليومية.

2. تبويب سجل الخيل العام (Horse Records)

محرك بحث ذكي: إمكانية البحث الفوري عن أي خيل للوصول إلى بياناته (جواز السفر، الشهادات، المرفقات الرسمية).

عرض الكروت (Card View): تحويل البيانات الجافة إلى كروت بصرية سهلة القراءة تدعم المرفقات والروابط المباشرة.

3. نظام التقارير الرسمي

وضع التقرير (Report Mode): عند طلب تصدير PDF، يقوم النظام تلقائياً بتفعيل وضع الطباعة الذي يزيل العناصر غير الضرورية (مثل الأزرار والفلاتر) ويضيف ترويسة وتذييلاً رسمياً للمستند.

🧠 تحليل منطق معالجة البيانات (Script logic)

يتميز الكود البرمجي في script.js بعدة حلول ذكية:

معالجة التواريخ (Date Parsing): يحتوي الكود على دالة مخصصة parseDMY لمعالجة التواريخ بصيغة (يوم/شهر/سنة). هذا يحل مشكلة المتصفحات التي تقلب اليوم والشهر تلقائياً إذا كانت القيم أقل من 12.

المزامنة المحلية (Local Time Sync): يتم استخدام دالة getLocalDateString لضمان أن تظليل التقويم يطابق التاريخ الفعلي المسجل، وتجنب أخطاء فرق التوقيت العالمي (UTC).

التجاوب مع الجوال: تم بناء الـ CSS والـ JS ليعملا بانسجام؛ حيث تظهر كروت الإحصائيات بنظام (2 في كل سطر) على الجوال لضمان وضوح الأرقام.

⚙️ طريقة التشغيل

تأكد من نشر ملفات Google Sheets الخاصة بك بصيغة CSV.

قم بتغيير روابط TRAINING_CSV و RECORDS_CSV في ملف script.js.

ارفع الملفات الثلاثة (index.html, style.css, script.js) على استضافة (مثل GitHub Pages).

تم تطوير هذا النظام لصالح مربط جادا للأصالة © 2026
